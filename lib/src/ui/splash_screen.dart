import 'package:flutter/material.dart';

//TODO:REFORMAT THE STRUCTURE OF THE APP WHEN DONE
// This is just to say that the app is maybe loading or waking up.
// so you could show some animations or sth like that.
class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Splash Screen'),
      ),
    );
  }
}
